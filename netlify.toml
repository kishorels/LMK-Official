[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "build"

# Handle client-side routing for React
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Node version (react-router-dom v7 requires Node 20+)
[build.environment]
  NODE_VERSION = "20"
