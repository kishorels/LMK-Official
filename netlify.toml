[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "build"

# Handle client-side routing for React
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Node version
[build.environment]
  NODE_VERSION = "18"
