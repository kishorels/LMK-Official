services:
  - type: web
    name: lmk-official
    env: static
    buildCommand: npm install --legacy-peer-deps && npm run build
    staticPublishPath: dist
    rootDir: frontend
    routes:
      # Rewrites all requests to index.html for React Router support
      - type: rewrite
        source: /*
        destination: /index.html
